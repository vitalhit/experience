
<script type="text/javascript">

	$(function(){
		var $element = '{{ user.mail }}';

		if ($element){
			// $('.secondname').val("{{ user.second_name }}");
			// $('.name').val("{{ user.name }}");
			// $('.phone').val("{{ user.phone }}");
			$('.id').val("{{ user.id }}");
		}
	});

</script>
