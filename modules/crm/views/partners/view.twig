<ul class="breadcrumb"><li><a href="/">Главная</a></li>
<li><a href="/crm/partners">Partners</a></li>
<li class="active">View</li>
<li><a href="/crm/partners/create">+</a></li>
</ul>       

<div class="row">
    <div class="col-xs-12 col-lg-3 left_blo">
        <div class="stiky">
            <div class="newblo">
                {% if model.image %}
                    <img src="/uploads/users/{{ model.image }}" class="img-responsive img-circle">
                {% else %}
                    <img src="/web/images/noimage.png" class="img-responsive img-circle">
                {% endif %}
                <h2 class="mt0">{% if model.name %}{{ model.name }} {{ model.second_name }}{% endif %}</h2>
                <a href="/crm/partners/update?id={{ model.id }}" class="btn btn-primary ma w120">Редактировать</a>
                <h4>User_id: {{ user_id }}<br>id: {{ model.id }}</h4>
                <p><b>Статус:</b> {{ model.status }}</p>
                <p><b>Дата рождения:</b> {{ model.birthday|date('d.m.Y') }}</p>
                <p><b>E-mail:</b> {{ model.mail }}</p>
                <p><b>Телефон:</b> {{ model.phone }}</p>
                <p><b>Пол:</b> {% if model.sex == 0 %} женщина {% else %} мужчина {% endif %} </p>
                <p><b>Группы:</b> {{ model.groups }}</p>
                <p><b>Откуда:</b> {{ model.froms.url }}</p>
                <p><b>Получать письма:</b> {% if model.sendmail == 1 %} Да {% else %} Нет {% endif %} </p>
                <p><b>Пожелания:</b> {{ model.vishes }}</p>
            </div>
            {% if model.info %}
                <div class="newblo"><p>{{ model.info }}</p></div>
            {% endif %}
        </div>
    </div>
    <div class="col-xs-12 col-lg-9">
        <div class="newblo">
            <div class="row">
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
            </div>
        </div>
        
       <!--  <div class="mini_header mt0"><span>ВИЗИТЫ <div class="badge">{{ model.visits|length }}</div></span></div>

        <div class="newblo">
            <table class="table stacktable">
                <tr>
                    <td>Начало</td>
                    <td>Окончание</td>
                    <td>Скидка</td>
                    <td>Стоимость</td>
                </tr>
                {% for row in model.visits|reverse %}
                    <tr>
                        <td>{{ row.start|date('d.m в H:i') }}</td>
                        <td>{{ row.end|date('d.m в H:i') }}</td>
                        <td>{{ row.discount_money }}</td>
                        <td>{{ row.money }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div> -->

    </div>
</div>