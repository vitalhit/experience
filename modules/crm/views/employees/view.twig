<div class="row">
    <div class="col-xs-12 col-lg-3 left_blo">
        <div class="stiky">
            <div class="newblo">
                {% if employee.image %}
                    <img src="/uploads/users/{{ employee.image }}" class="img-responsive img-circle">
                {% else %}
                    <img src="/images/noimage.png" class="img-responsive img-circle">
                {% endif %}
                <h2 class="mt0">{% if employee.name %}{{ employee.name }} {{ employee.second_name }}{% endif %}</h2>
                <a href="/crm/employees/update?id={{ employee.id }}" class="btn btn-primary ma w120">Редактировать</a>
                <h4>User_id: {{ employee.user_id }}<br>Профиль id: {{ employee.id }}</h4>
                <p><b>Статус:</b> {{ employee.status }}</p>
                <p><b>Дата рождения:</b> {{ employee.birthday|date('d.m.Y') }}</p>
                <p><b>E-mail:</b> {{ employee.mail }}</p>
                <p><b>Телефон:</b> {{ employee.phone }}</p>
                <p><b>Пол:</b> {% if employee.sex == 0 %} женщина {% else %} мужчина {% endif %} </p>
                <p><b>Группы:</b> {{ employee.groups }}</p>
                <p><b>Откуда:</b> {{ employee.froms.url }}</p>
                <p><b>Получать письма:</b> {% if employee.sendmail == 1 %} Да {% else %} Нет {% endif %} </p>
                <p><b>Пожелания:</b> {{ employee.vishes }}</p>
            </div>
            {% if employee.info %}
                <div class="newblo"><p>{{ employee.info }}</p></div>
            {% endif %}
        </div>
    </div>
    <div class="col-xs-12 col-lg-9">
        <div class="newblo">
            <div class="row">
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
                <div class="col-xs-3"><h3 class="itog"></h3></div>
            </div>
        </div>
        
       <!--  <div class="mini_header mt0"><span>ВИЗИТЫ <div class="badge">{{ employee.visits|length }}</div></span></div>

        <div class="newblo">
            <table class="table stacktable">
                <tr>
                    <td>Начало</td>
                    <td>Окончание</td>
                    <td>Скидка</td>
                    <td>Стоимость</td>
                </tr>
                {% for row in employee.visits|reverse %}
                    <tr>
                        <td>{{ row.start|date('d.m в H:i') }}</td>
                        <td>{{ row.end|date('d.m в H:i') }}</td>
                        <td>{{ row.discount_money }}</td>
                        <td>{{ row.money }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div> -->

    </div>
</div>