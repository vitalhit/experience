{{ use('/yii/widgets') }}

<ul class="search_result"></ul>

<div class="row">
    <div class="col-xs-12 col-lg-12">
        <div class="newblo">
            <div class="row">
              
                <div class="col-xs-12 col-lg-12">
                    <h2>Все Цитаты</h2>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xs-12 col-lg-3 left_blo">
        
    </div>

    <div class="col-xs-12 col-lg-9">
        
        <a class="btn btn-success btn-sm" href="/crm/quote/create"> добавить цитату <span class="glyphicon glyphicon-plus small"></span></a>
        <br><br>
        <table class="tb stacktable">
            <tr>

                <td>id</td>
                <td>author</td>
                <td>text</td>
                <td>status</td>
            </tr>
            {% for one in all %}
            <tr>

                <td>{{ one.id}}</td>
                <td>{{ one.author}}</td>
                <td>{{ one.text}}</td>
                <td>{{ one.status}}</td>
            </tr>
            {% endfor %}
        </table>
    </div>
</div>

    {{link_pager_widget({
        'pagination': pages
    }) }}



    <div class="popup end popup_d"></div>

    <div class="overlay"></div>

