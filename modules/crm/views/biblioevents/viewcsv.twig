{% for tik in tickets %};{{ tik.persons.name }};{{ tik.persons.second_name }};{{ tik.barcode }};{{ tik.seats.name }}{% if tik.seat %}{{ tik.seat }}{% endif %};{{ tik.money }};{{tik.promocode}};{{ tik.persons.mail }};{{ tik.persons.phone}}
{% endfor %}