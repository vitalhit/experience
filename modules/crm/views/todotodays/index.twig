{{ use('/yii/widgets') }}

<ul class="search_result"></ul>

<div class="row">
    <div class="col-xs-12 col-lg-12">
        <div class="newblo">
            <div class="row">
              
                <div class="col-xs-12 col-lg-12">
                    <h2>Все ежедневные задачки</h2>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xs-12 col-lg-12">
        
        <a class="btn btn-success btn-sm" href="/crm/todotodays/create"> добавить задачку <span class="glyphicon glyphicon-plus small"></span></a>
        <br><br>
        <table class="tb stacktable">
            <tr>
                <td>id</td>
                <td>title</td>
                <td>link</td>
                <td>info</td>
                <td>status</td>
                <td>done</td>
                <td>today</td>
                <td>to&nbsp;do</td>
            </tr>
            {% for todotoday in todotodays %}
            
            {% if  todotoday.status > 0 and todotoday.status < 100 and todotoday.date_last|date("Ymd") != now|date("Ymd") and todotoday.weekday_first <= now|date("N") and  now|date("N") <= todotoday.weekday_last  %}
            <tr>
                <td>{{todotoday.id}}</td>
                <td><a href="/crm/todotodays/update?id={{todotoday.id}}">{{todotoday.title}}</a></td>

                <td><a href="{{todotoday.link}}">{{todotoday.link}}</a></td>
                <td>{{todotoday.info}}</td>
                <td>{{todotoday.status}}</td>
                
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id %}
                            {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </td>
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id and row.date_create|date('Y-m-d') == now|date('Y-m-d')%} 
                        {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}

                        {% endif %}
                    {% endfor %}
                </td>
                <td><span class="btn btn-success btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}">add</a></span> <span class="btn btn-warning btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}&status=-1">don't do</a></span></td>
            </tr>
            {% endif %}
            {% endfor %}
            <tr>
                <td colspan="8">
                    <br><h4>1 раз в неделю или 1 раз в месяц</h4>
                </td>
            </tr>
            <tr>
                <td>id</td>
                <td>title</td>
                <td>link</td>
                <td>info</td>
                <td>status</td>
                <td>done</td>
                <td>today</td>
                <td>to&nbsp;do

            </tr>
            {% for todotoday in todotodays %}
            {% if todotoday.monthday == now|date('d') or   todotoday.weekday == now|date('N') %}
            <tr>
                <td>{{todotoday.id}}</td>
                <td><a href="/crm/todotodays/update?id={{todotoday.id}}">{{todotoday.title}}</a></td>

                <td><a href="{{todotoday.link}}">{{todotoday.link}}</a></td>
                <td>{{todotoday.info}}</td>
                <td>{{todotoday.status}}</td>
                
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id %} 
                        <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>

                        {% endif %}
                    {% endfor %}
                </td>
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id and row.date_create|date('Y-m-d') == now|date('Y-m-d')%} 
                       <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>

                        {% endif %}
                    {% endfor %}
                </td>
                <td><span class="btn btn-success btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}">add</a></span> <span class="btn btn-warning btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}&status=-1">don't do</a></span></td>
            </tr>
             {% endif %}
            {% endfor %}
            <tr>
                <td colspan="8">
                    <br><h4>Сделанные сегодня</h4>
                </td>
            </tr>
            {% for row in steps %}
                {% for todotoday in todotodays %}
                    {% if row.todotoday_id == todotoday.id and row.date_create|date('Y-m-d') == now|date('Y-m-d')%} 
            <tr>
                <td>{{todotoday.id}}</td>
                <td><a href="/crm/todotodays/update?id={{todotoday.id}}">{{todotoday.title}}</a></td>

                <td><a href="{{todotoday.link}}">{{todotoday.link}}</a></td>
                <td>{{todotoday.info}}</td>
                <td>{{todotoday.status}}</td>
                
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id %} 
                    {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}

                        {% endif %}
                    {% endfor %}
                </td>
                <td>
                       {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}
                </td>
                <td><span class="btn btn-success btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}">add</a></span> <span class="btn btn-warning btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}&status=-1">don't do</a></span></td>
            </tr>


                    {% endif %}   
                {% endfor %}        
            {% endfor %}
            <tr><td colspan="8"> <br><h4>Задачи на паузе</h4></td></tr>
            {% for todotoday in todotodays %}
            {% if todotoday.status == 0 %}
            <tr>
                <td>{{todotoday.id}}</td>
                <td><a href="/crm/todotodays/update?id={{todotoday.id}}">{{todotoday.title}}</a></td>

                <td><a href="{{todotoday.link}}">{{todotoday.link}}</a></td>
                <td>{{todotoday.info}}</td>
                <td>{{todotoday.status}}</td>
                
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id %}
                            {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </td>
                <td>   
                    {% for row in steps %}

                        {% if row.todotoday_id == todotoday.id and row.date_create|date('Y-m-d') == now|date('Y-m-d')%} 
                        {% if row.status == 1 %} 
                    <span class="btn btn-success btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% elseif row.status == -1 %}
                    <span class="btn btn-warning btn-sm">{{ row.date_create|date('Y-m-d H:i') }} ({{row.user_id}})</span>
                            {% endif %}

                        {% endif %}
                    {% endfor %}
                </td>
                <td><span class="btn btn-success btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}">add</a></span> <span class="btn btn-warning btn-sm"><a  class="white" href="/crm/todotodays/add?id={{todotoday.id}}&status=-1">don't do</a></span></td>
            </tr>
            {% endif %}
            {% endfor %}

        </table>
    </div>
</div>

    {{link_pager_widget({
        'pagination': pages
    }) }}



    <div class="popup end popup_d"></div>

    <div class="overlay"></div>



<script type="text/javascript">

    $(function(){

    //Живой поиск
    $('.who').bind("change keyup input click", function() {
        if(this.value.length >= 4){
            $.ajax({
                type: 'get',
                    url: "/crm/persons/personsfind", //Путь к обработчику
                    data: {'name':this.value},
                    response: 'text',
                    success: function(data){
                        $(".search_result").html(data).fadeIn(); //Выводим полученые данные в списке
                    }
                })
            $(".overlay").fadeIn();
            $(".form_lite").css({'zIndex': 9000});

        }
    })

    $(".search_result").hover(function(){
        $(".who").blur(); //Убираем фокус с input
    })

    //При выборе результата поиска, прячем список и заносим выбранный результат в input
    $(".search_result").on("click", "li", function(){
        s_user = $(this).text();
        //$(".who").val(s_user).attr('disabled', 'disabled'); //деактивируем input, если нужно
        $(".search_result, .overlay").fadeperson();
        $(".form_lite").css({'zIndex': 1000});
    })

})
</script>